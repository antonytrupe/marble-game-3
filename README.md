scoop bucket add extras  
scoop install godot-mono


# building

## setup
in godot ui, make sure the export templates are downloaded
in godot  
make sure the command godot-mono is in path  
use a bash terminal(git bash or similiar)

## running
run ./build.sh

# deploying

## setup
download/install steamworks sdk
set environment variable STEAMCMD to the path of the steam builder  
example path: "C:\Users\anton\Desktop\steamworks_sdk_162\sdk\tools\ContentBuilder\builder\steamcmd.exe"

## running

